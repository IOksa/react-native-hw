export const selectIsLoggedIn = state => state.isLoggedIn;

export const selectUserName = state => state.name;
export const selectUserEmail = state => state.email;
export const selectUserAvatar = state => state.avatar;

export const selectIsRefreshing = state => state.isRefreshing;

export const selectError = state => state.error;

export const selectIsLoading = state => state.isLoading;